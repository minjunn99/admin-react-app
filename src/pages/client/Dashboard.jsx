// Import library
import React from "react";

// Import component

const Dashboard = () => {
    return (
        <section className="flow">
            <h1>Dashboard</h1>
        </section>
    );
};

export default Dashboard;
